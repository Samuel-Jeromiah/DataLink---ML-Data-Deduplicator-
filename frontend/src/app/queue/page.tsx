"use client";
import ConfirmedMatches from '../matches/page';

// Reusing identical structured component logic for Review Queue
export default function ReviewQueue() {
  return <ConfirmedMatches />;
}
